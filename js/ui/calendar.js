import {state} from '../core/state.js';
export function initCalendar(){document.getElementById('tab-month').onclick=()=>sel('month');document.getElementById('tab-all').onclick=()=>sel('all');renderMonth();}function sel(which){const mBtn=document.getElementById('tab-month');const aBtn=document.getElementById('tab-all');mBtn.setAttribute('aria-selected', which==='month');aBtn.setAttribute('aria-selected', which==='all');document.getElementById('calendar-wrap').classList.toggle('hidden', which!=='month');document.getElementById('all-grid').classList.toggle('hidden', which!=='all');if(which==='all')renderAll();else renderMonth();}function renderMonth(){const wrap=document.getElementById('calendar-wrap');wrap.innerHTML='';const now=new Date();const y=now.getFullYear();const m=now.getMonth();const first=new Date(y,m,1).getDay();const days=new Date(y,m+1,0).getDate();const grid=document.createElement('div');grid.className='month';for(let i=0;i<first;i++){const b=document.createElement('div');b.className='day';b.style.visibility='hidden';grid.appendChild(b);}for(let d=1;d<=days;d++){const key=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;const div=document.createElement('div');div.className='day';if(state.moments[key]){div.classList.add('has');if(state.moments[key].photo){const img=new Image();img.src=state.moments[key].photo;div.appendChild(img);}}if(d===now.getDate())div.classList.add('today');div.textContent=d;grid.appendChild(div);}wrap.appendChild(grid);}function renderAll(){const grid=document.getElementById('all-grid');grid.innerHTML='';Object.keys(state.moments).sort().forEach(k=>{const m=state.moments[k];const card=document.createElement('div');card.className='card';if(m.photo){const img=new Image();img.src=m.photo;img.className='photo';card.appendChild(img);}const body=document.createElement('div');body.className='body';body.innerHTML='<div class='date'>'+new Date(k).toDateString()+'</div><div>'+(m.caption||'')+'</div>';card.appendChild(body);grid.appendChild(card);});}