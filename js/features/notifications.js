export function initNotifications(){const sw='self.addEventListener('install',e=>self.skipWaiting()); self.addEventListener('activate',e=>self.clients.claim());';const url=URL.createObjectURL(new Blob([sw],{type:'text/javascript'}));if('serviceWorker'in navigator){navigator.serviceWorker.register(url);}setInterval(()=>{const t='20:00'.split(':').map(n=>parseInt(n,10));const now=new Date();if(now.getHours()===t[0]&&now.getMinutes()===t[1]&&now.getSeconds()<2){const el=document.getElementById('toast');el.textContent='Time to capture today!';el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2200);}},1000);} 